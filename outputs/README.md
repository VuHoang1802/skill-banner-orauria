# Kết quả đã tạo

Không đặt dữ liệu đầu vào trong thư mục này. Mỗi lần chạy ghi kết quả vào `outputs/<batch-id>/<product-id>/prompts.txt`. Mỗi prompt nằm trên đúng một dòng và không có dòng trống giữa các prompt. File `batch-summary.yaml` tại thư mục batch ghi nguồn và kết quả kiểm tra.
