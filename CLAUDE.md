# Dự án tạo prompt banner theo thương hiệu

Đọc `PROJECT.md` trước khi làm việc. Kiểm tra `brand/brand-profile.yaml`; nếu `onboarding.status` chưa là `complete`, làm theo `instructions/ONBOARDING.md`. Mỗi lần chỉ hỏi tối đa năm câu liên quan. Lưu mọi thông tin đã xác minh trong `brand/`; không xem lịch sử trò chuyện là nguồn dữ liệu lâu dài.
